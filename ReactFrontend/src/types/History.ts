export type Note= {
    note_id: string;
    title: string;
    content: string;
    created_at: string;
  }

  export type HistoryDetail= {
    filePath: string;
    // Add other properties of historyDetail if any
  }

  export type HistoryNoteData= {
    note_id: string;
    title: string;
    content: string;
    created_at: string;
  }